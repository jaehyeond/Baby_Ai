# Task.md - 작업 추적

**최종 업데이트**: 2025-01-19

---

## 🎯 Project Vision: Developmental Cognitive Architecture

> **"Pre-trained Knowledge + Developmental Learning Mechanisms = Adaptive Intelligence"**

### 핵심 철학

**"Baby부터" vs "고등학생부터"는 잘못된 이분법입니다.**

| 기존 논쟁 | 우리의 해답 |
|-----------|-------------|
| LLM Scaling (OpenAI) vs World Models (LeCun) | **Both are right - Hybrid approach** |
| 아기처럼 무지하게 시작? | ❌ 아님 |
| 고등학생 수준 지식부터? | ❌ 이것도 부분적 |
| **우리의 접근** | **LLM 지식 + 발달적 학습 메커니즘** |

### 차별화 포인트

| 기존 LLM | Fine-Tuning | **Neural A2A** |
|----------|-------------|----------------|
| 정적 지식 | 추가 학습 | **동적 시냅스 가소성** |
| 단일 모델 | 단일 모델 | **Dual Process (System 1/2)** |
| 무감정 | 무감정 | **감정 기반 기억 강화** |
| 무발달 | 무발달 | **6단계 발달 (NEWBORN→ADOLESCENT)** |

### "Baby"는 은유다

- ❌ 지식이 없다는 의미가 **아님**
- ✅ **학습하고 성장하는 방식**이 아기와 같다는 의미
- Gemini/GPT의 지식은 그대로 활용
- 그 위에 뇌과학 기반 **발달적 메커니즘** 추가

### 업계 트렌드 정렬

- **LeCun's World Models (2025)**: LLM의 physical grounding 부족 지적 → 우리: 경험 기반 학습
- **DeepMind Embodied AI**: Gemini Robotics, SIMA 2 → 우리: 추상적 인지 도메인 적용
- **Developmental Robotics**: Linda Smith의 embodiment hypothesis → 우리: 발달 단계 구현

### 핵심 질문에 대한 답변

> **Q: "RAG도 있고 Transformer가 있는데 그거보다 좋아?"**
>
> A: **다른 문제를 풀고 있습니다.** GPT는 10TB 데이터가 필요하지만, 인간 아기는 3년 만에 언어를 배웁니다. 우리는 그 차이를 연구합니다.

> **Q: "왜 이걸 해?"**
>
> A: **AGI로 가는 길이 다르기 때문입니다.** Transformer는 '더 큰 뇌'를 만들지만, 우리는 '뇌가 자라는 방법'을 연구합니다. 스케일링의 한계를 돌파하려면 학습 방식 자체를 바꿔야 합니다.

### 장기 로드맵 (5 Phases)

```
Phase 1 (현재 ✅): 대시보드 + 기본 학습
Phase 2 (다음): World Model 통합 (상상/예측)
Phase 3: 진짜 Emotion Engine (감정→행동)
Phase 4: Mobile Embodied AI (카메라/마이크)
Phase 5: 자율 목표 설정 (스스로 뭘 배울지 결정)
```

📖 **상세 문서**: [task_baby_brain.md](task_baby_brain.md#-research-roadmap-2026-01-20-수립)

---

## 현재 상태

| 단계 | 작업 | 상태 |
|------|------|------|
| Phase 1 | 프로젝트 스캐폴딩 생성 | ✅ 완료 |
| Phase 1 | 환경 설정 (가상환경, 의존성) | ✅ 완료 |
| Phase 2 | Coder Agent 구현 | ✅ 완료 |
| Phase 3 | CLI Client 구현 | ✅ 완료 |
| Phase 4 | 통합 테스트 | ✅ 완료 |
| Phase 5 | Tester Agent 구현 | ✅ 완료 |
| Phase 5 | Reviewer Agent 구현 | ✅ 완료 |
| Phase 6 | Orchestrator 구현 | ✅ 완료 |
| Phase 6 | 멀티에이전트 파이프라인 테스트 | ✅ 완료 |
| **Phase 7** | **NeuralPipeline (ANN 기반 레이어 구조)** | ✅ 완료 |
| **Phase 7.5** | **NeuralSubstrate (단일 프로세스 실행)** | ✅ 완료 |
| **Phase 7.6** | **Baby AI (발달 AI 시스템)** | ✅ 완료 |
| **Phase 7.7** | **Baby AI CLI & Persistence** | ✅ 완료 |
| **Phase 7.8** | **Memory Storage Migration (JSON→Supabase)** | ✅ 완료 |
| **Phase 7.8.1** | **Cognitive Router (멀티모델 라우팅)** | ✅ 완료 |
| **Phase 7.9** | **Frontend Visualization (3D 뇌 + Baby 아바타)** | 📋 계획 |
| **Phase 8** | **Textual Backpropagation (텍스트 역전파)** | 🔄 진행예정 |
| Phase 9 | Self-Evolution Engine (자기 진화) | ⏳ 대기 |
| Phase 10 | Team Optimization (팀 최적화) | 📋 계획 |
| Phase 11 | Persistent Substrate (영속 기반) | 📋 계획 |

---

## 🧠 Neural A2A: Agentic Neural Network 기반 멀티에이전트 시스템

> "A2A 에이전트 자체가 뉴럴 네트워크 - 에이전트=뉴런, A2A 연결=시냅스, 텍스트 피드백=그래디언트"

### 핵심 패러다임 전환

| 기존 접근 | 새로운 접근 (ANN) |
|-----------|------------------|
| 뇌 "기능" 모방 | 뇌 "구조+학습" 모방 |
| 파이프라인 (선형) | 레이어드 네트워크 |
| SQLite 중앙집중 | 분산 (각 노드) |
| 규칙 기반 학습 | **텍스트 역전파** |
| 수동 진화 | **자동 Self-Evolution** |

### 핵심 원칙

> **A2A 프로토콜 자체가 뉴럴 네트워크 구조다**
> - 에이전트 = 뉴런
> - A2A 연결 = 시냅스
> - 텍스트 피드백 = 그래디언트
> - Self-Evolution = 학습

### 참고 논문
- [Agentic Neural Networks (arXiv:2506.09046, 2025)](https://arxiv.org/abs/2506.09046)
- Textual Backpropagation: 자연어 피드백으로 에이전트 프롬프트/역할/조정 개선

---

## 🧬 ANN 기반 Neural A2A 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        AGENTIC NEURAL NETWORK (ANN)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ══════════════════════ FORWARD PASS (순전파) ══════════════════════        │
│                                                                              │
│  Layer 0: INPUT LAYER                                                        │
│  ┌─────────────────┐                                                         │
│  │     Router      │ ← 태스크 분류 및 분해                                   │
│  │  (Gemini Flash) │                                                         │
│  │    Port 9996    │                                                         │
│  └────────┬────────┘                                                         │
│           │                                                                  │
│  Layer 1: EXECUTIVE LAYER (전두엽)                                           │
│  ┌─────────────────┐  ┌─────────────────┐                                   │
│  │    Planner      │  │    Monitor      │ ← 계획/감시 팀                    │
│  │  (Claude Opus)  │  │ (Claude Sonnet) │                                   │
│  │    Port 9995    │  │    Port 9994    │                                   │
│  └────────┬────────┘  └────────┬────────┘                                   │
│           │                    │                                             │
│  Layer 2: PROCESSING LAYER (소뇌)                                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐              │
│  │    Coder_1      │  │    Coder_2      │  │    Executor     │ ← 실행 팀   │
│  │ (Claude Sonnet) │  │ (Gemini Pro)    │  │ (Claude Sonnet) │              │
│  │    Port 9999    │  │    Port 9998    │  │    Port 9993    │              │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘              │
│           │                    │                    │                        │
│  Layer 3: EVALUATION LAYER (안와전두피질)                                    │
│  ┌─────────────────┐  ┌─────────────────┐                                   │
│  │     Tester      │  │    Reviewer     │ ← 평가 팀                         │
│  │ (Claude Sonnet) │  │ (Claude Sonnet) │                                   │
│  │    Port 9998    │  │    Port 9997    │                                   │
│  └────────┬────────┘  └────────┬────────┘                                   │
│           │                    │                                             │
│  Layer 4: OUTPUT LAYER                                                       │
│  ┌─────────────────────────────────────────────────────────────┐            │
│  │                      Aggregator                              │            │
│  │                   (결과 통합 + 피드백 수집)                   │            │
│  └──────────────────────────┬──────────────────────────────────┘            │
│                             │                                                │
│                             ▼                                                │
│                      OUTPUT + FEEDBACK                                       │
│                             │                                                │
│  ══════════════════ BACKWARD PASS (역전파) ══════════════════════           │
│                             │                                                │
│  ┌──────────────────────────▼──────────────────────────────────┐            │
│  │              TEXTUAL BACKPROPAGATION ENGINE                  │            │
│  │                                                              │            │
│  │  1. 성공/실패 피드백 수집                                    │            │
│  │  2. 각 레이어별 "텍스트 그래디언트" 계산                     │            │
│  │  3. 프롬프트/역할/연결가중치 업데이트                        │            │
│  │  4. 팀 구성 자동 조정 (Self-Evolution)                       │            │
│  └──────────────────────────────────────────────────────────────┘            │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                      PERSISTENT SUBSTRATE (영속 기반)                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐              │
│  │ learned_prompts │  │   weights.db    │  │  episodes.db    │              │
│  │     .json       │  │   (SQLite)      │  │   (SQLite)      │              │
│  │ (학습된 프롬프트) │  │ (연결 가중치)   │  │ (에피소드 기록) │              │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 레이어별 역할

| Layer | 이름 | 역할 | 에이전트 | 뇌 영역 |
|-------|------|------|----------|---------|
| 0 | Input | 태스크 분류/분해 | Router | 시상 |
| 1 | Executive | 계획/감시 | Planner, Monitor | 전전두엽, ACC |
| 2 | Processing | 실행/코딩 | Coder₁, Coder₂, Executor | 소뇌 |
| 3 | Evaluation | 테스트/리뷰 | Tester, Reviewer | 안와전두피질 |
| 4 | Output | 결과 통합 | Aggregator | - |

### Forward/Backward Pass

```
FORWARD PASS (순전파):
Request → Layer 0 → Layer 1 → Layer 2 → Layer 3 → Layer 4 → Output

BACKWARD PASS (역전파 = Textual Backpropagation):
Feedback → 텍스트 그래디언트 계산 → 각 레이어 프롬프트/역할 업데이트
```

---

## 단계적 확장 계획 (ANN 기반 재설계)

### Phase 7: NeuralPipeline ✅ (완료)

기존 `FeedbackLoopPipeline` → `NeuralPipeline`으로 재설계

- [x] `NeuralLayer` 클래스 (레이어 추상화)
- [x] `NeuralPipeline` 클래스 (Forward Pass)
- [x] `LayerStatus` 열거형 정의
- [x] 레이어 간 연결 가중치 구조
- [x] 기본 피드백 루프 (최대 3회)
- [x] `Neuromodulator` 클래스 (전략 조정)
- [x] CLI 진입점 (`python -m neural`)

**구현된 파일**:
- `neural/__init__.py` - 패키지 초기화
- `neural/layer.py` - NeuralLayer, AgentNode, LayerConfig
- `neural/pipeline.py` - NeuralPipeline, Neuromodulator, ForwardResult
- `neural/__main__.py` - CLI 진입점

**실행 방법**:
```bash
# 에이전트 실행 후
python -m neural "피보나치 함수를 작성해줘"
python -m neural "퀵소트 구현" --iterations 5
```

**Forward Pass 흐름**:
```
Request
    │
    ▼
┌─────────┐
│ Layer 0 │ Router (분류)
└────┬────┘
     │
     ▼
┌─────────┐
│ Layer 1 │ Planner + Monitor (계획)
└────┬────┘
     │
     ▼
┌─────────┐
│ Layer 2 │ Coder Team (실행)
└────┬────┘
     │
     ▼
┌─────────┐
│ Layer 3 │ Tester + Reviewer (평가)
└────┬────┘
     │
     ▼
Output + Feedback → [실패시 Layer 2로 재시도]
```

### Phase 7.6: Baby AI (발달 AI 시스템) ✅ 완료

> "아기처럼 백지 상태에서 시작, 호기심으로 탐험, 감정으로 중요성 판단, 경험에서 학습"

**핵심 개념**:
- **발달 AI (Developmental AI)**: 인간 아기의 인지 발달 과정 모방
- **내재적 호기심 (Intrinsic Curiosity Module)**: 예측 오류 = 학습 신호 (arXiv:1705.05363)
- **학습 가능 영역 (Zone of Proximal Development)**: 너무 쉽지도 어렵지도 않은 최적 학습 영역
- **감정 기반 기억**: 감정이 강할수록 더 잘 기억 (편도체 역할)

**컴포넌트 구조**:
```
neural/baby/
├── __init__.py          # 패키지 초기화
├── emotions.py          # EmotionalCore - 6가지 기본 감정
├── curiosity.py         # CuriosityEngine - ICM 기반 내재적 동기
├── memory.py            # MemorySystem - 3중 기억 시스템
├── development.py       # DevelopmentTracker - 발달 단계 추적
├── self_model.py        # SelfModel - 자아 인식
├── substrate.py         # BabySubstrate - 통합 실행 환경
└── test_baby.py         # 단위 테스트 (7개 모두 통과)
```

**1. EmotionalCore (감정 핵심)**:
```python
class EmotionType(Enum):
    CURIOSITY = "curiosity"      # 호기심 - 탐험 동기
    JOY = "joy"                  # 기쁨 - 성공 보상
    FEAR = "fear"                # 두려움 - 위험 회피
    SURPRISE = "surprise"        # 놀람 - 예측 오류
    FRUSTRATION = "frustration"  # 좌절 - 반복 실패
    BOREDOM = "boredom"          # 지루함 - 너무 쉬움

# 감정이 행동에 영향
core.modulate_action("generate")  # → "cautious_generate" (두려움 높을 때)
core.get_exploration_rate()       # 탐험 비율 (epsilon-greedy)
```

**2. CuriosityEngine (호기심 엔진)**:
```python
class LearningZone(Enum):
    TOO_EASY = "too_easy"     # 지루함 영역 → 보상 감소
    PROXIMAL = "proximal"     # 최적 학습 영역 → 보상 최대
    TOO_HARD = "too_hard"     # 좌절 영역 → 보상 크게 감소

# 호기심 신호 계산
signal = engine.compute_simple_curiosity(success=True, output="...", task_type="function")
# → CuriositySignal(prediction_error, learning_progress, novelty, intrinsic_reward, zone)
```

**3. MemorySystem (3중 기억 시스템)**:
```python
# 해마 (Hippocampus) - 에피소드 기억
episodic.store(Experience(...))
episodic.recall_similar("피보나치")

# 피질 (Cortex) - 의미 기억
semantic.add_concept("python", category="language")
semantic.find_related("coding")

# 소뇌 (Cerebellum) - 절차 기억
procedural.record_step("function", "analyze", success=True)
procedural.get_best_approach("function")  # 가장 성공적인 접근법
```

**4. DevelopmentStage (발달 단계)**:
```python
class DevelopmentStage(Enum):
    NEWBORN = 0   # 신생아: 반사적 반응
    INFANT = 1    # 유아: 패턴 인식
    BABY = 2      # 아기: 모방 학습
    TODDLER = 3   # 걸음마: 언어, "왜?" 질문
    CHILD = 4     # 어린이: 자아 인식, 마음 이론

# 발달에 따라 능력 해금
tracker.has_capability("language")  # TODDLER 이상에서만 True
tracker.get_learning_rate_modifier()  # 어린 단계일수록 빠른 학습
```

**5. SelfModel (자아 모델)**:
```python
self_model.register_capability("coding", "코드 작성")
self_model.update_capability("coding", success=True)
self_model.get_capability("coding")  # → Capability(confidence=0.8, ...)

self_model.update_preference("python", positive_outcome=True)
self_model.likes("python")  # → True

self_model.add_limitation("complex_math")
self_model.generate_self_description()  # "나는 coding을 잘하고 python을 좋아해요."
```

**테스트 실행**:
```bash
cd e:\A2A\our-a2a-project
.\.venv\Scripts\activate
python -m neural.baby.test_baby

# 결과: 7 passed, 0 failed
```

---

### Phase 7.7: Baby AI CLI & Persistence ✅ (완료)

> "Baby AI를 CLI에서 실행하고 경험이 세션 간 지속되도록 구현"

**완료된 작업**:
- [x] `--baby` 플래그 CLI 지원 (`python -m neural "요청" --baby`)
- [x] 메모리 영속화 경로 설정 (`.baby_memory/`)
- [x] 세션 간 경험 누적 (`experience_counter` 지속)
- [x] 발달 상태 저장/복원 (`state.json`)
- [x] UTF-8 인코딩 수정 (한국어 기억 저장)

**실행 방법**:
```bash
cd e:\A2A\our-a2a-project
.\.venv\Scripts\activate
python -m neural "피보나치 함수" --baby
python -m neural "퀵소트 구현" --baby   # 경험 누적됨
python -m neural "스택 클래스" --baby   # 발달 진행됨
```

**메모리 저장 구조**:
```
.baby_memory/
├── state.json       # 발달 단계, 경험 카운터, 자아 모델
├── episodic.json    # 에피소드 기억 (경험)
├── semantic.json    # 의미 기억 (개념)
└── procedural.json  # 절차 기억 (패턴)
```

**현재 상태** (2025-12-30):
- Experience Count: 2 (퀵소트, 스택 클래스)
- Development Stage: 0 (NEWBORN)
- Progress: 20%
- Success Rate: 50%

---

### Phase 7.8: Memory Storage Migration ✅ (완료)

> "JSON → Supabase pgvector로 메모리 시스템 업그레이드"

**완료된 작업** (2025-12-31):

**DB 스키마 v008 - Neural Connections (뇌 연결 구조)**

기존 v007은 6개 테이블이 독립적인 "섬"이었음. v008에서 뇌과학 기반 연결 추가:

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    v008: NEURAL CONNECTIONS                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  AMYGDALA ↔ HIPPOCAMPUS (감정-경험 연결)                                 │
│  ┌─────────────────┐         ┌─────────────────┐                        │
│  │  emotion_logs   │ ───FK── │   experiences   │                        │
│  │ +experience_id  │         │+emotion_snapshot│                        │
│  └─────────────────┘         └─────────────────┘                        │
│                                      │                                   │
│  CORTEX (지식 그래프)                 │ M:N                              │
│  ┌─────────────────┐         ┌──────┴──────────┐                        │
│  │semantic_concepts│←───────→│experience_concepts│ (Hebb's Law)         │
│  │                 │         └─────────────────┘                        │
│  │                 │                                                     │
│  │                 │←───────→│ concept_relations │ (Knowledge Graph)    │
│  └─────────────────┘         └─────────────────┘                        │
│                                                                          │
│  BASAL GANGLIA (절차 학습)                                               │
│  ┌─────────────────┐         ┌─────────────────┐                        │
│  │procedural_patterns│←─────│pattern_learning_events│                   │
│  └─────────────────┘         └─────────────────┘                        │
│                                      │                                   │
│                              ┌───────┴───────┐                          │
│                              │  experiences  │ (reward signal)          │
│                              └───────────────┘                          │
│                                                                          │
│  SYNAPTIC FUNCTIONS (시냅스 강화/약화)                                   │
│  • strengthen_experience_concept_link() - Hebb's Law                    │
│  • strengthen_concept_relation()        - 관계 강화                      │
│  • decay_all_connections()              - 시냅스 약화                    │
│  • find_associated_concepts()           - 연관 개념 검색                 │
│  • boost_memory_by_emotion()            - 감정 기반 기억 강화            │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**v008 마이그레이션 내역**:
| 버전 | 테이블/함수 | 설명 |
|------|-------------|------|
| v008a | emotion_logs.experience_id, experiences.emotion_snapshot | 감정↔경험 FK 연결 |
| v008b | experience_concepts | 경험↔개념 M:N (Hebb's Law - co_activation_count) |
| v008c | concept_relations | 개념 간 관계 (is_a, has_a, similar_to 등) |
| v008d | pattern_learning_events | 절차 학습 이벤트 (reward_signal, prediction_error) |
| v008e | 시냅스 함수 5개 | 강화/약화/검색 함수들 |

**뇌과학 원리 적용**:
- **Hebb's Law**: "함께 발화하는 뉴런은 함께 연결된다" → `co_activation_count`
- **LTP/LTD**: 장기 강화/약화 → `strengthen_*()` / `decay_all_connections()`
- **Emotional Salience**: 감정적 경험이 더 강하게 기억됨 → `boost_memory_by_emotion()`

**Python Supabase 연동** (2025-12-31 완료):

새로 생성된 파일:
- `neural/baby/db.py` - Supabase 클라이언트 및 CRUD 작업
- `neural/baby/embeddings.py` - OpenAI text-embedding-3-small 벡터 생성
- `neural/baby/migrate_to_supabase.py` - JSON → Supabase 마이그레이션 스크립트

수정된 파일:
- `neural/baby/memory.py` - Supabase 연동 추가 (로컬 JSON 폴백 유지)
- `.env` - SUPABASE_URL, SUPABASE_ANON_KEY, OPENAI_API_KEY 추가
- `pyproject.toml` - supabase, openai 의존성 추가

**사용법**:
```python
from neural.baby.memory import MemorySystem

# Supabase 모드 (기본값)
memory = MemorySystem(use_supabase=True)

# 경험 저장 → Supabase experiences 테이블 + 로컬 캐시
exp = memory.record_experience(
    request="피보나치 함수 구현",
    action="def fibonacci(n): ...",
    outcome="success",
    success=True,
    emotion_snapshot={"curiosity": 0.8, "dominant": "curiosity"}
)

# 벡터 유사도 검색 (OpenAI 임베딩 + pgvector)
similar = memory.episodic.recall_similar("재귀 함수", n=3)

# 로컬 전용 모드
memory_local = MemorySystem(use_supabase=False, storage_path=".baby_memory/")
```

**현재 DB 상태**:
- experiences: 6개+ (with 1536-dim embeddings)
- semantic_concepts: 3개+
- procedural_patterns: 2개+
- baby_state: 1개 (싱글톤)

---

### Phase 7.8.1: Cognitive Router ✅ (완료)

> "인간 뇌의 이중 처리 시스템(Dual Process Theory)을 모방한 멀티모델 라우팅"

**핵심 개념**:
- **System 1 (Fast/Intuitive)**: Gemini Flash → 빠른 응답, 단순 작업
- **System 2 (Slow/Analytical)**: GPT-5.2 Thinking → 깊은 추론, 복잡한 분석

**새로 생성된 파일**:
- `neural/baby/llm_client.py` - 멀티모델 LLM 클라이언트 (OpenAI + Google)
- `neural/baby/cognitive_router.py` - 발달 단계 기반 라우팅 로직
- `neural/baby/test_cognitive_router.py` - 라우팅 테스트

**발달 단계별 모델 분배**:
| Stage | Flash | Standard | Thinking |
|-------|-------|----------|----------|
| NEWBORN | 95% | 4% | 1% |
| INFANT | 85% | 12% | 3% |
| TODDLER | 70% | 22% | 8% |
| CHILD | 50% | 30% | 20% |
| ADOLESCENT | 40% | 35% | 25% |

**라우팅 고려 요소**:
1. **작업 복잡도**: TRIVIAL → CRITICAL (키워드 기반 분석)
2. **발달 단계**: 어릴수록 Flash 위주, 성장하면 Thinking 비중 증가
3. **감정 상태**: 높은 호기심 → 모델 업그레이드, 높은 좌절감 → Thinking 전환
4. **이전 실패**: 2회+ 실패 → Thinking (high) 모드
5. **응답 긴급도**: IMMEDIATE → Flash 강제

**BabySubstrate 통합**:
```python
# substrate.py에 CognitiveRouter 통합됨
# 매 요청마다 최적 모델 자동 선택

# 예: NEWBORN 단계에서 단순 요청
[ROUTER] 간단한 작업 → Flash 사용
[MODEL] gemini-2-flash (thinking: None)

# 예: CHILD 단계에서 복잡한 요청
[ROUTER] 복잡한 작업 + CHILD 단계 → Thinking (medium)
[MODEL] gpt-5.2-thinking (thinking: medium)
```

**비용 최적화**:
- Flash (95%) 사용 시 비용 계수: 0.1
- Thinking (high) 사용 시 비용 계수: 5.0
- 발달 단계 기반 하이브리드로 ~88% 비용 절감 가능

---

### Phase 7.9: Frontend Visualization 📋 (계획)

> "3D 뇌 시각화 + Baby AI 아바타 + 실시간 감정/발달 대시보드"

**왜 프론트엔드가 필요한가?**
| 측면 | 가치 |
|------|------|
| **교육적** | 발달 AI 개념을 시각적으로 이해 |
| **디버깅** | AI가 왜 그 결정을 했는지 추적 |
| **포트폴리오** | 면접/발표에서 "보여줄 수 있는" 결과물 |
| **실험** | 다양한 입력에 대한 반응 관찰 |

**기술 스택 선정** (Flutter vs Next.js 비교 후 결정):
```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND STACK                            │
├─────────────────────────────────────────────────────────────┤
│  Framework:     Next.js 14 (App Router)                     │
│  3D Engine:     React Three Fiber + Drei                    │
│  Animation:     Framer Motion + react-spring                │
│  Charts:        Recharts (레이더 차트)                       │
│  Styling:       Tailwind CSS                                 │
│  Realtime:      Supabase Realtime                           │
│  Deployment:    Vercel (무료)                               │
└─────────────────────────────────────────────────────────────┘
```

**핵심 시각화 컴포넌트**:

1. **🧠 Brain3D** (React Three Fiber)
   - 뉴런 노드 (Sphere) - 에이전트별 색상
   - 시냅스 연결 (Line) - 가중치에 따른 두께
   - 활성화 glow 애니메이션

2. **👶 BabyAvatar** (SVG + Framer Motion)
   - 감정별 표정 6가지 (호기심 🤔, 기쁨 😊, 두려움 😰, 놀람 😲, 좌절 😤, 지루함 😑)
   - 발달 단계별 외형 변화 (NEWBORN → CHILD)

3. **💭 EmotionRadar** (Recharts)
   - 6각형 레이더 차트
   - 실시간 감정 강도 표시

4. **📊 DevelopmentProgress**
   - 5단계 프로그레스 바
   - 마일스톤 체크포인트
   - 경험 카운터

5. **🔄 PipelineFlow**
   - Forward Pass 시각화
   - Layer 0 → 1 → 2 → 3 → Output
   - 에이전트 상태 표시

**프로젝트 구조**:
```
frontend/
├── app/
│   ├── page.tsx              # 메인 대시보드
│   ├── brain/page.tsx        # 3D 뇌 시각화
│   └── baby/page.tsx         # Baby AI 캐릭터
├── components/
│   ├── brain/
│   │   ├── Brain3D.tsx       # React Three Fiber 3D 뇌
│   │   ├── NeuronNode.tsx    # 뉴런 (Sphere)
│   │   └── SynapseLink.tsx   # 시냅스 (Line)
│   ├── baby/
│   │   ├── BabyAvatar.tsx    # SVG 아기 캐릭터
│   │   └── expressions/      # 감정별 표정
│   ├── dashboard/
│   │   ├── EmotionRadar.tsx  # 감정 레이더 차트
│   │   └── DevelopmentBar.tsx
│   └── pipeline/
│       └── PipelineFlow.tsx  # Forward Pass 시각화
└── lib/
    └── supabase.ts           # Realtime 구독
```

**데이터 흐름**:
```
Python CLI (Baby AI)
        │
        ▼ Write
┌───────────────────┐
│  Supabase DB      │
│  (pgvector)       │
└───────┬───────────┘
        │ Realtime 구독
        ▼
Next.js Frontend (3D 렌더링)
```

**단계적 개발 계획**:
- [ ] **F1**: Next.js 프로젝트 설정 + Supabase 연결
- [ ] **F2**: 2D 대시보드 (감정 레이더, 발달 프로그레스)
- [ ] **F3**: Baby 아바타 (SVG + 표정 6가지)
- [ ] **F4**: 3D 뇌 시각화 (React Three Fiber)
- [ ] **F5**: 실시간 파이프라인 시각화

**참고 리소스**:
- [React Three Fiber 뉴럴 네트워크 튜토리얼](https://sbcode.net/react-three-fiber/neural-network/)
- [TensorSpace - 3D 뉴럴 네트워크 시각화](https://github.com/tensorspace-team/tensorspace)
- [Next.js + Python 대시보드 가이드](https://www.augustinfotech.com/blogs/building-an-interactive-dashboard-with-next-js-and-python/)

---

### Phase 8: Textual Backpropagation ⏳

피드백을 "텍스트 그래디언트"로 변환하여 학습

- [ ] `TextualGradient` 클래스
- [ ] 피드백 → 그래디언트 변환 로직
- [ ] 프롬프트 자동 업데이트
- [ ] 역할(role) 동적 조정
- [ ] 연결 가중치 학습

**핵심 개념**:
```python
# 텍스트 역전파 예시
feedback = "코드에 에러 핸들링이 부족함"
gradient = analyze_feedback(feedback)  # → {"error_handling": -0.3}
update_prompt(coder, gradient)  # 프롬프트에 에러 핸들링 강조 추가
```

### Phase 9: Self-Evolution Engine ⏳

에이전트가 스스로 진화

- [ ] 역할 자동 조정
- [ ] 새로운 에이전트 팀 자동 생성
- [ ] 성능 기반 팀 구성 최적화
- [ ] 에이전트 프롬프트 자동 개선

### Phase 10: Team Optimization 📋

팀 구성 최적화

- [ ] 병렬 실행 지원 (같은 레이어 내)
- [ ] 동적 팀 크기 조정
- [ ] Attention 메커니즘 (어떤 에이전트에 집중할지)
- [ ] 에이전트 간 협력 패턴 학습

### Phase 11: Persistent Substrate 📋

학습 결과 영속화

- [ ] 학습된 프롬프트 저장 (JSON)
- [ ] 연결 가중치 저장 (SQLite)
- [ ] 에피소드 기록 (SQLite)
- [ ] 세션 간 학습 상태 복원

---

## 시스템 구성도 (ANN 기반 재설계)

```
our-a2a-project/
│
├── agents/                      # A2A 서버 에이전트 (뉴런)
│   ├── coder/                   # Layer 2: 코드 생성 (Port 9999)
│   ├── tester/                  # Layer 3: 테스트 (Port 9998)
│   ├── reviewer/                # Layer 3: 리뷰 (Port 9997)
│   ├── router/                  # Layer 0: 라우팅 (Port 9996)
│   ├── planner/                 # Layer 1: 계획 (Port 9995)
│   ├── monitor/                 # Layer 1: 감시 (Port 9994)
│   └── executor/                # Layer 2: 실행 (Port 9993)
│
├── neural/                      # 🆕 ANN 프레임워크
│   ├── __init__.py
│   ├── layer.py                 # NeuralLayer 클래스
│   ├── pipeline.py              # NeuralPipeline (Forward Pass)
│   ├── substrate.py             # NeuralSubstrate (단일 프로세스)
│   ├── visualizer.py            # ASCII 시각화
│   ├── neuromodulator.py        # 신경조절 (도파민 등)
│   ├── backprop.py              # Textual Backpropagation (예정)
│   ├── evolution.py             # Self-Evolution Engine (예정)
│   ├── weights.py               # 연결 가중치 관리 (예정)
│   │
│   └── baby/                    # 🆕 Baby AI (발달 AI)
│       ├── __init__.py
│       ├── emotions.py          # EmotionalCore
│       ├── curiosity.py         # CuriosityEngine (ICM)
│       ├── memory.py            # MemorySystem (3중 기억)
│       ├── development.py       # DevelopmentTracker
│       ├── self_model.py        # SelfModel (자아 인식)
│       ├── substrate.py         # BabySubstrate
│       └── test_baby.py         # 단위 테스트
│
├── hosts/                       # A2A 클라이언트
│   ├── cli/                     # 단일 에이전트 대화
│   └── orchestrator/            # 멀티에이전트 오케스트레이터
│       ├── agent_client.py      # A2A 클라이언트 래퍼
│       ├── pipeline.py          # 기존 파이프라인 (호환용)
│       └── __main__.py
│
├── substrate/                   # 🆕 영속 기반
│   ├── prompts/                 # 학습된 프롬프트
│   │   └── learned_prompts.json
│   └── db/                      # SQLite 저장소
│       ├── weights.db           # 연결 가중치
│       └── episodes.db          # 에피소드 기록
│
├── common/                      # 공통 유틸리티
│   ├── config.py
│   └── llm_factory.py
│
└── docs/                        # 문서
    ├── NEURAL_A2A_ARCHITECTURE.md
    └── ANN_DESIGN.md            # 🆕 ANN 상세 설계
```

---

## 에이전트 요약

### 현재 구현된 에이전트 (Phase 6 완료)

| Layer | 이름 | 포트 | 역할 | LLM |
|-------|------|------|------|-----|
| 2 | Coder Agent | 9999 | 코드 생성 | Claude Sonnet |
| 3 | Tester Agent | 9998 | 코드 테스트 | Claude Sonnet |
| 3 | Reviewer Agent | 9997 | 코드 리뷰 | Claude Sonnet |

### 계획된 에이전트 (Phase 9+)

| Layer | 이름 | 포트 | 역할 | LLM |
|-------|------|------|------|-----|
| 0 | Router Agent | 9996 | 태스크 분류 | Gemini Flash |
| 1 | Planner Agent | 9995 | 계획 수립 | Claude Opus |
| 1 | Monitor Agent | 9994 | 품질 감시 | Claude Sonnet |
| 2 | Executor Agent | 9993 | 코드 실행 | Claude Sonnet |

---

## 완료된 작업

### ✅ Phase 1-4: 기본 구조 및 Coder Agent
- pyproject.toml, 디렉토리 구조, 환경설정
- Coder Agent (Claude API 코드 생성)
- CLI Client (대화형)
- 기본 통합 테스트 완료

### ✅ Phase 5: 에이전트 확장
- Tester Agent: 코드 실행 + Claude 분석
- Reviewer Agent: 품질/보안/스타일 리뷰

### ✅ Phase 6: 오케스트레이션
- Orchestrator: 멀티에이전트 파이프라인
- 테스트 결과: 3개 에이전트 연동 성공

---

## 실행 가이드

### 전체 시스템 실행 (3개 터미널 필요)
```bash
# Terminal 1: Coder Agent (Layer 2)
cd e:\A2A\our-a2a-project
.\.venv\Scripts\activate
python -m agents.coder --port 9999

# Terminal 2: Tester Agent (Layer 3)
python -m agents.tester --port 9998

# Terminal 3: Reviewer Agent (Layer 3)
python -m agents.reviewer --port 9997
```

### Orchestrator 실행 (4번째 터미널)
```bash
python -m hosts.orchestrator "피보나치 함수를 작성해줘"
```

---

## 참고 자료

### 핵심 논문
- [Agentic Neural Networks (arXiv:2506.09046)](https://arxiv.org/abs/2506.09046)
- [Top 5 AI Agent Architectures 2025](https://www.marktechpost.com/2025/11/15/comparing-the-top-5-ai-agent-architectures-in-2025-hierarchical-swarm-meta-learning-modular-evolutionary/)
- [Brain-inspired SNN for Multi-Agent](https://www.sciencedirect.com/science/article/pii/S2666389923001265)

### 패턴 참고
- [Awesome Agentic Patterns](https://github.com/nibzard/awesome-agentic-patterns)
- [Multi-Agent Orchestration Patterns](https://github.com/aianytime/multi-agents-orchestration-design-patterns)

---

## 다음 세션 시작 프롬프트

### 옵션 1: Python Supabase 연동 (권장 - 다음 단계)

```
Neural A2A 프로젝트 계속

프로젝트: e:\A2A\our-a2a-project
@Task.md 읽어줘
@SQL_task.md 읽어줘

완료된 작업:
- Phase 7.8: Supabase DB 스키마 v008 완료 (Neural Connections)
  - 12개 마이그레이션 적용 (v001~v008e)
  - 6개 기본 테이블 + 3개 연결 테이블 + 5개 시냅스 함수
  - 현재 모든 테이블 비어있음 (baby_state: 1개 행만 존재)

현재 상태:
- Python은 여전히 .baby_memory/ JSON 파일 사용 중
- Supabase는 준비 완료, 데이터만 없음

다음 작업: Python → Supabase 연동
1. supabase-py 설치 및 클라이언트 설정
2. neural/baby/memory.py 수정
   - EpisodicMemory: experiences 테이블 연동
   - SemanticMemory: semantic_concepts + experience_concepts 연동
   - ProceduralMemory: procedural_patterns + pattern_learning_events 연동
3. OpenAI text-embedding-3-small로 벡터 임베딩 생성
4. 기존 .baby_memory/ 데이터 마이그레이션
```

### 옵션 2: Frontend Visualization 시작

```
Neural A2A 프로젝트 - 프론트엔드 시각화

프로젝트: e:\A2A\our-a2a-project
@Task.md 읽어줘 (Phase 7.9 섹션)

목표: Baby AI를 시각적으로 보여주는 웹 대시보드 구축

기술 스택:
- Next.js 14 + TypeScript
- React Three Fiber (3D 뇌 시각화)
- Recharts (감정 레이더 차트)
- Framer Motion (애니메이션)
- Supabase Realtime (실시간 업데이트)

첫 번째 작업:
1. frontend/ 폴더에 Next.js 프로젝트 생성
2. Supabase 연결 설정
3. 감정 레이더 차트 MVP 구현
```

### 옵션 3: Phase 8 (Textual Backpropagation) 진행

```
Neural A2A 프로젝트 계속

프로젝트: e:\A2A\our-a2a-project
@Task.md 읽어줘

다음 작업: Phase 8 - Textual Backpropagation
- 텍스트 피드백을 "그래디언트"로 변환
- 프롬프트 자동 업데이트
- 연결 가중치 학습

참고: Supabase DB 연동이 먼저 완료되어야 학습 결과를 영속화할 수 있음
```

### 옵션 4: Baby AI 추가 실험

```
Neural A2A 프로젝트 - Baby AI 실험

프로젝트: e:\A2A\our-a2a-project

Baby AI 실행:
python -m neural "피보나치 함수" --baby
python -m neural "이진탐색 구현" --baby
python -m neural "링크드리스트 클래스" --baby

현재 상태 확인:
- .baby_memory/state.json: 발달 상태
- .baby_memory/episodic.json: 경험 기록

목표:
- 더 많은 경험 축적
- NEWBORN → INFANT 발달 단계 진입
- 패턴 인식 마일스톤 달성
```
